{
  "name": "hkai-sms-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node --env-file=.env src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.6.2",
    "pg": "^8.16.3"
  }
}
